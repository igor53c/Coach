<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="igra.paket.coach">

    <uses-permission android:name="android.permission.WAKE_LOCK" />

    <uses-feature android:name="android.hardware.type.watch" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Coach">
        <activity android:name=".view.GlavniActivity" />
        <activity android:name=".view.BiranjeKlubaActivity" />
        <activity android:name=".view.ObjasnjenjeActivity" />
        <activity android:name=".view.PredMecActivity" />
        <activity android:name=".view.ZamenaIgracaActivity" />
        <activity android:name=".view.TransferActivity" />
        <activity android:name=".view.UvodActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>